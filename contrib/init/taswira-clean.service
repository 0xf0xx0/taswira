[Unit]
Description=delete old taswira uploads

[Service]
# delete images older than 30 days
# TODO: atime? delete old unaccessed images ig
ExecStart=/bin/find /var/lib/taswira -daystart -atime 30 -name '*.png'
